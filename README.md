# 자산관리 대시보드

`Overview.xlsx`를 직접 업로드해서 월별 순자산/투자원금/자산구성을 확인하는 정적 웹 대시보드입니다.

## 사용 방법

1. `dashboard/index.html` 파일을 브라우저에서 엽니다.
2. 상단 `엑셀 가져오기` 버튼으로 `Overview.xlsx`를 선택합니다.
3. 업로드 즉시 대시보드가 갱신됩니다.

## 참고

- 현재 대시보드는 `data.js` 사전 생성 없이 동작합니다.
- 엑셀 시트(`주식관리`)의 병합 헤더(예: `B1~F1`)도 업로드 파서에서 처리합니다.
